// normal way
function sum(number1, number2) {
    let result = number1 + number2;
    return result;
}
const output = sum(10, 15);
console.log(output);
console.log(result); // here result is not define so here we use scope