// normal way if else
var i = -5;
if (0 > i) {
    console.log("below zero");
} else {
    console.log(i);
}

// in javascript ,by default 0 and empty sting && null && NaN && false is falsy value in javascript so sometime u don't need condition like n>0 ...